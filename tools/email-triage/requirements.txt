# Core
pydantic>=2.0

# Claude backend (recommended). Not needed if you only use the Ollama backend.
anthropic>=0.69

# Gmail access. Not needed for --demo or if you pipe in your own emails.
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# Optional: load ANTHROPIC_API_KEY etc. from a local .env file.
python-dotenv>=1.0

# The Ollama backend uses only the Python standard library (urllib) — no extra deps.
